(()=>{const APP_VERSION="100",FLAG=`little_labels_pwa_recovery_${APP_VERSION}`;function updateVisibleBuild(){document.querySelectorAll('.footer-note span').forEach(el=>{if(/App build\s+\d+/i.test(el.textContent||''))el.textContent=`App build ${APP_VERSION}`;});}async function recover(){updateVisibleBuild();if(!('serviceWorker'in navigator))return;try{if(!sessionStorage.getItem(FLAG)){sessionStorage.setItem(FLAG,'1');const regs=await navigator.serviceWorker.getRegistrations();for(const reg of regs){try{await reg.update();}catch{}try{await reg.unregister();}catch{}}if('caches'in window){const keys=await caches.keys();await Promise.all(keys.filter(k=>k.includes('label-maker-')||k.startsWith('little-labels-')).map(k=>caches.delete(k)));}const url=new URL(location.href);url.searchParams.set('appv',APP_VERSION);location.replace(url.toString());}}catch(err){console.warn(err);}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',updateVisibleBuild,{once:true});else updateVisibleBuild();addEventListener('load',()=>setTimeout(recover,100));navigator.serviceWorker?.addEventListener?.('message',e=>{if(e.data?.type==='LL_SW_REMOVED'&&!sessionStorage.getItem(`${FLAG}_msg`)){sessionStorage.setItem(`${FLAG}_msg`,'1');location.reload();}});})();