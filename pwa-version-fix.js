(()=>{const APP_VERSION="101";function updateVisibleBuild(){document.querySelectorAll('.footer-note span').forEach(el=>{if(/App build\s+\d+/i.test(el.textContent||''))el.textContent=`App build ${APP_VERSION}`;});}async function ensureWorker(){updateVisibleBuild();if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register(`./service-worker.js?v=${APP_VERSION}`,{updateViaCache:'none'});await reg.update().catch(()=>{});}catch(err){console.warn(err);}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',updateVisibleBuild,{once:true});else updateVisibleBuild();addEventListener('load',()=>setTimeout(ensureWorker,150));})();