(()=>{const APP_VERSION="98";function updateVisibleBuild(){document.querySelectorAll('.footer-note span').forEach(el=>{if(/App build\s+\d+/i.test(el.textContent||''))el.textContent=`App build ${APP_VERSION}`;});}async function keepWorkerStable(){updateVisibleBuild();if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register(`./service-worker.js?v=${APP_VERSION}`,{updateViaCache:'none'});reg.update().catch(()=>{});}catch(err){console.warn('Little Labels service worker update skipped:',err);}updateVisibleBuild();}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',updateVisibleBuild,{once:true});else updateVisibleBuild();addEventListener('load',()=>setTimeout(keepWorkerStable,250));})();