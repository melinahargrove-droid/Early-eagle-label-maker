(()=>{const APP_VERSION="49",REFRESH_FLAG=`eea_force_refresh_v${APP_VERSION}`;function updateVisibleBuild(){document.querySelectorAll('.footer-note span').forEach(el=>{if(/App build\s+\d+/i.test(el.textContent||''))el.textContent=`App build ${APP_VERSION}`;});}async function forceCurrentWorker(){updateVisibleBuild();if(!('serviceWorker'in navigator))return;try{const regs=await navigator.serviceWorker.getRegistrations();for(const reg of regs)await reg.unregister();if('caches'in window){const keys=await caches.keys();await Promise.all(keys.filter(k=>k.startsWith('early-eagle-label-maker-')).map(k=>caches.delete(k)));}const reg=await navigator.serviceWorker.register(`./service-worker.js?v=${APP_VERSION}`,{updateViaCache:'none'});await reg.update().catch(()=>{});const url=new URL(location.href);if(url.searchParams.get('appv')!==APP_VERSION&&!sessionStorage.getItem(REFRESH_FLAG)){sessionStorage.setItem(REFRESH_FLAG,'1');url.searchParams.set('appv',APP_VERSION);location.replace(url.toString());return;}updateVisibleBuild();}catch(err){console.warn(err);updateVisibleBuild();}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',updateVisibleBuild,{once:true});else updateVisibleBuild();addEventListener('load',()=>setTimeout(forceCurrentWorker,100));})();