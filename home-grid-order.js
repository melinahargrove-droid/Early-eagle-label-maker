(()=>{function arrange(){const grid=document.querySelector('.home-create-grid');if(!grid)return;const cards=[...grid.querySelectorAll('.home-action')];const find=t=>cards.find(x=>(x.textContent||'').toLowerCase().includes(t));const photo=find('photo'),typed=find('type'),list=find('list'),product=find('product'),name=find('name labels');const order=[photo,typed,list,name,product].filter(Boolean);order.forEach(x=>grid.append(x));if(name)name.classList.add('ll-name-home');}function install(){arrange();setTimeout(arrange,250);setTimeout(arrange,800)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install()})();