(()=>{function clean(){const confirm=document.getElementById('confirm');if(!confirm||confirm.classList.contains('hidden'))return;const confidence=(window.identification?.confidence||'').toLowerCase();const notes=window.identification?.notes||'';const els=[...confirm.querySelectorAll('p,.muted,.tiny')];els.forEach(el=>{const t=(el.textContent||'').trim();const isMeta=/^(Category:|Confidence:)/i.test(t);const isNote=notes&&t===notes.trim();if(isNote){el.style.display=(confidence==='high'||!notes.trim())?'none':''}else if(!isMeta&&notes&&t.includes(notes.trim())&&confidence==='high'){el.style.display='none'}})}function install(){const c=document.getElementById('confirm');if(c)new MutationObserver(()=>requestAnimationFrame(clean)).observe(c,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});document.getElementById('confirmYes')?.addEventListener('click',clean);clean()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install()})();